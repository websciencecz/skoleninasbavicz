version: '3'

services:
  web:
    image: php:7.3-apache
    volumes:
      - ./:/var/www
    ports:
      - 80
    environment:
      VIRTUAL_HOST: skoleninasbavi.cz.test

    working_dir: /var/www

  composer:
    image: composer:latest
    volumes:
      - ./:/app
    command: install

  assets:
    image: node:latest
    volumes:
      - ./:/app
    working_dir: /app
    command: npm install -D

networks:
  default:
    external:
      name: nginx-proxy

