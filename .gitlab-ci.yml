stages:
    - build

composer:
    stage: build
    image: composer:latest
    script:
        - composer install
        
generate:
    stage: build
    image: php7.1-cli
    script:
        - vendor/bin/statie generate source
   